import * as executionFn from './gantt-execution-task.functions';

export const toMap = executionFn.toMap;

export const collapseAll = executionFn.collapseAll;

export const toggleCollapse = executionFn.toggleCollapse;

export const toggleActivation = executionFn.toggleActivation;

export const hide = executionFn.hide;

export const spreadItem = executionFn.spreadItem;

export const sort = executionFn.sort;

export const isPhase = executionFn.isPhase;

export const isCommission = executionFn.isCommission;

export const isProject = executionFn.isProject;
